[pytest]
addopts = -q
markers =
    smoke: quick checks for critical paths
    regression: broader coverage tests
    negative: negative scenarios
    pim: OrangeHRM PIM module tests
testpaths = tests

